# Ansible Requirements
# Install with: ansible-galaxy install -r requirements.yml

collections:
  - name: kubernetes.core
    version: ">=2.0.0"
  
  - name: amazon.aws
    version: ">=5.0.0"

roles: []
